AOS.init({
    duration: 500,   // ระยะเวลาเริ่มต้น (ms)
    offset: 150,      // ระยะห่างเริ่มต้น (px)
    easing: 'ease-in-out', // รูปแบบการเคลื่อนไหว
    once: true,       // ทำงานครั้งเดียว
    mirror: false,     // ทำงานย้อนกลับ
  });
